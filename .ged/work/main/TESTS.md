# Tests: Task-scoped governance kernel — slice 3

## Focused

- `npm test -- tests/governance-store.test.ts tests/governance.test.ts tests/ged-paths.test.ts`
- `npm run check`
- `npm run lint`

## Completion gates

- `npm run format`
- `npm run verify`
- `npm audit --audit-level=high`
- `git diff --check`

## Scenario acceptance

- Initialization creates revision zero for the exact generated work ID.
- CAS accepts the current revision and rejects stale/identity-changing writes.
- Parallel evidence appends retain both records and reject duplicate IDs.
- Unknown schemas, malformed fields, and missing work metadata fail closed.
- Deleted/stale projection can be regenerated byte-for-byte from JSON state.
