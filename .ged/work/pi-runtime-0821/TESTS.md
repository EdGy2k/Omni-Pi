# TESTS: Update Pi runtime and companion dependencies

- `npm run check`
- `npm run lint`
- `npm test`
- `npm run pack:check`
- Confirm the installed dependency tree resolves the intended versions without invalid peers.
